{% extends 'base.html.twig' %}

{% block title %}Liste des nationalités
{% endblock %}

{% block body %}
	<script src="https://kit.fontawesome.com/54e9e3700c.js" crossorigin="anonymous"></script>

	<div class="container-fluid pt-3">
		<div class="row">
			<div class="col-lg-12">
				<div class="card mb-4">
					<div class="card-body p-3">
						<div class="mt-3 text-center">
							<h1 class="h1"> Liste des Nationalités</h1>
							<a href="{{ path('app_nationality_new') }}" class="btn btn-info">
									<i class="fa-solid fa-plus"></i>
									Ajouter une catégorie</a>
						</div>

						<div class="rg-4 pt-5 table-responsive">
							<table class="table table-striped table-hover text-center">
								<thead class="table-dark">
									<tr>
										<th>Nationalité</th>
										<th>Actions</th>
									</tr>
								</thead>
								<tbody>
									{% set sortedNationalities = nationalities|sort((a, b) => a.name <=> b.name) %}
									{% for nationality in sortedNationalities %}
										<tr>
											<td class="align-middle">{{ nationality.name }}</td>
											<td class="align-middle">
												<div class="d-flex justify-content-center align-items-center">
													<div class="btn-group me-2" role="group">
														<a href="{{ path('app_nationality_edit', {'id': nationality.id}) }}" class="btn btn-warning">
															<i class="fa-solid fa-pen"></i>
														</a>
													</div>
													<div class="btn-group me-2" role="group">
														<form action="{{ path('app_nationality_delete', {'id': nationality.id}) }}" method="post" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette nationalité ?')">
															<input type="hidden" name="_method" value="DELETE">
															<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nationality.id) }}">
															<button type="submit" class="btn btn-danger">
																<i class="fa-solid fa-trash"></i>
															</button>
														</form>
													</div>
												</div>
											</td>
										</tr>
									{% else %}
										<tr>
											<td colspan="4">Il n'y a aucune nationalité !</td>
										</tr>
									{% endfor %}
								</tbody>
							</table>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
