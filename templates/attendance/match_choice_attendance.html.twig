{% extends 'base.html.twig' %}

{% block title %}
    Page d'appel - {{ category|upper }}
{% endblock %}

{% block body %}
    <link href="/css/attendance.css" rel="stylesheet"/>
    <script src="/js/jquery-3.3.1.min.js"></script>
    <script src="/js/bootstrap.js"></script>

    <div class="col-lg-8 mx-auto p-5 py-md-5">
        <div class="mt-3 text-center">
            <h1 class="h1 mb-2 display-4">Équipes - {{ category|upper }}</h1>
        </div>

        <div class="text-center d-grid gap-2 col-xl-4 col-md-6 mx-auto mt-5 mb-5">
        {% set sortedTeams = teams|sort((a, b) => a.name <=> b.name) %}
            {% for team in sortedTeams %}
                {# <a href="{{ path('app_team_detail', {'id': team.id}) }}" class="btn btn-info btn-block">{{ team.name }}</a> #}
                <a href="/appel/match/{{ category }}/{{ team.name }}/{{ team.id }}" class="btn btn-info">{{ team.name }}</a>
            {% endfor %}
		</div>

        <div class="g-4 pt-3 pb-3">
            <div class="col text-center" style="text-align: start;">
                <a href="/" class="btn btn-info">
                    <i class="fa-solid fa-house"></i>
                    Accueil</a>
            </div>
        </div>        
    </div>

{% endblock %}
