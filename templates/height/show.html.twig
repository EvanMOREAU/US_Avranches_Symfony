{% extends 'base.html.twig' %}

{% block title %}Height{% endblock %}

{% block body %}
    <h1>Height</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ height.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ height.value }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ height.date ? height.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_height_index') }}">back to list</a>

    <a href="{{ path('app_height_edit', {'id': height.id}) }}">edit</a>

    {{ include('height/_delete_form.html.twig') }}
{% endblock %}
