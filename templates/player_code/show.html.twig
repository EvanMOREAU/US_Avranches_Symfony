{% extends 'base.html.twig' %}

{% block title %}PlayerCode{% endblock %}

{% block body %}
    <h1>PlayerCode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ player_code.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ player_code.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_player_code_index') }}">back to list</a>

    <a href="{{ path('app_player_code_edit', {'id': player_code.id}) }}">edit</a>

    {{ include('player_code/_delete_form.html.twig') }}
{% endblock %}
