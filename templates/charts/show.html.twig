{% extends 'base.html.twig' %}

{% block title %}ChartConfiguration{% endblock %}

{% block body %}
    <h1>ChartConfiguration</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chart_configuration.id }}</td>
            </tr>
            <tr>
                <th>ChartType</th>
                <td>{{ chart_configuration.chartType }}</td>
            </tr>
            <tr>
                <th>ConfigData</th>
                <td>{{ chart_configuration.configData ? chart_configuration.configData|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_charts_index') }}">back to list</a>

    <a href="{{ path('app_charts_edit', {'id': chart_configuration.id}) }}">edit</a>

    {{ include('charts/_delete_form.html.twig') }}
{% endblock %}
