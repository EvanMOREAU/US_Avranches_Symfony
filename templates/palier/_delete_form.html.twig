<form method="post" action="{{ path('app_palier_delete', {'id': palier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ palier.id) }}">
    <button class="btn">Delete</button>
</form>
