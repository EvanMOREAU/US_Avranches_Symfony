
<form method="post" action="{{ path('app_palier_delete', {'id': palier.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce palier ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ palier.id) }}">
    <button type="submit" class="btn btn-danger">
        <i class="fas fa-trash"></i>
    </button>
</form>


