{% extends 'base.html.twig' %}

{% block title %}Palier{% endblock %}

{% block body %}
    <h1>Palier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Numéro</th>
                <td>{{ palier.numero }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ palier.name }}</td>
            </tr>
            <tr>
                <th>Objectif</th>
                <td>{{ palier.objectif }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_palier_index') }}">back to list</a>

    <a href="{{ path('app_palier_edit', {'id': palier.id}) }}">edit</a>

    {{ include('palier/_delete_form.html.twig') }}
{% endblock %}
